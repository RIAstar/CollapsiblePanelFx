type = 'swc'

def stylePath = projectDir.path + '/src/main/resources/'

dependencies {
	external project(':collapsible-panel-fx')
}

additionalCompilerOptions.add("-include-stylesheet=defaults.css,${stylePath}defaults.css")
